---
label: Blog post
hide_body: false
fields:
- type: text
  name: layout
  label: layout
  default: cambermast-blog-post
  config:
    required: true
  hidden: true
- type: text
  name: categories
  label: categories
  config:
    required: true
  default: agile-blog
  hidden: true
- type: text
  name: title
  label: title
  config:
    required: true
- name: featuredImage
  type: file
  config:
    maxSize: 250
  label: Featured image
  description: Select an image to display at the top of the post.
  default: ''
- type: datetime
  name: date
  label: date
  config:
    required: true
  default: now
  hidden: false
